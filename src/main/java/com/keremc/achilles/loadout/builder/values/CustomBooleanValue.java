package com.keremc.achilles.loadout.builder.values;

import org.bukkit.Material;

public class CustomBooleanValue extends CustomValue<Boolean> {
    public CustomBooleanValue(Material display, Boolean data, String name) {
        super(display, data, name);
    }
}
